index=qts_dev_apps host=xaapktprxp sourcetype=rds-adapter "KafkaEventProcessor processed"
| rex field=message "(?i)processed\s+(?<events>\d+)\s+events;\s+elapsedTimeInMillis=(?<millis>\d+)"
| eval seconds = millis / 1000
| eval throughput = events / seconds
| stats avg(throughput) as "Avg Msg/sec"
